# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Laufzeit-/Nutzerdaten (nicht in den Quellcode)
profile-data/
logs/

# Build-Artefakte (alles unter _build\ via build.ps1; Alt-Pfade zur Sicherheit mit drin)
_build/
build/
dist/
Output/
*.manifest
Cipher-Setup-*.exe

# Secrets (z. B. spätere Backend-/API-Keys)
.env
*.secret
secrets.*

# Generierter Code-Dump (Referenz, kein Quellcode)
cipher-python-code.txt

# OS / Editor
Thumbs.db
.DS_Store
.idea/
.vscode/
